const initialize = {
    itemList: [],
    loading: false,
}
function reducer(state = initialize, action) {
    switch (action.type) {
        default:
            return state
    }
}

export default reducer;